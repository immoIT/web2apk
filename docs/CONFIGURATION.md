# Configuration

Configuration is primarily controlled by `config.yml` and optional GitHub Actions variables.

This file was moved out of `app/src/main/res/values/` because Android resource directories only accept resource files with supported `.xml` filenames.
